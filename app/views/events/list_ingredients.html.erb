<div class="container">

  <div class="d-flex justify-content-between mb-3 pt-5">
    <div class="subtitles align-self-center">
      <p> </p>
      <p> </p>
    </div>
    <div class="align-self-center">
      <h2 class="text-center"><%= @event.title %></h2>
      <p class="subtitles text-center"> <%= @event.number_of_members %> members</p>
    </div>
     <div class="d-flex my-3">
      <div class="ml-auto p-0"><%= link_to " ↩︎ Back", event_path, id: :btn_back %></div>
    </div>
  </div>

  <div class="mx-auto">
    <div class="form-check w-50 text-center">
      <div class="list-group" id="list-tab" role="tablist">
        <% @ingredients_hash.each do |ingredient_id, ammount| %>
          <p class="list-group-item list-group-item-action" id="list-home-list" role="tab" aria-controls="home">
            <input class="form-check-input" type="checkbox" value="meat" id="defaultCheck1">
            <label class="form-check-label" for="defaultCheck1">
              <%= Ingredient.find(ingredient_id).name %>: <%= "#{ammount} #{Ingredient.find(ingredient_id).recipe_ingredients.first.unit}" %>
            </label>
          </p>
        <% end %>
      </div>
    </div>
  </div>


</div>
