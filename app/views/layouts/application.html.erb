<!DOCTYPE html>
<html>
  <head>

    <% if content_for?(:metadata) %>
      <%= yield :metadata %>
    <% else %>
      <title>ReadyMeals - Plan Your Meals</title>
      <meta description="ReadyMeals is a platform that allows you to plan for meals, whether it be a single one or a whole trip full of exiting opportunities, we make meal planning, easy planning.">
      <!-- Facebook Open Graph data -->
      <meta name="og:title" content="ReadyMeals - Plan Your Meals" />
      <meta name="og:type" content="website" />
      <meta name="og:url" content="readymeal.asia" />
      <meta name="og:image" content="Background_og.png" />
      <meta name="og:description" content="ReadyMeals is a platform that allows you to plan for meals, whether it be a single one or a whole trip full of exiting opportunities, we make meal planning, easy planning." />
      <meta name="og:site_name" content="Ready Meals" />
    <% end %>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <meta charset="utf-8">

    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <link rel='shortcut icon' type='image/x-icon' href='/favicon.ico' />
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload', defer: true %>
  </head>

  <body>
    <div id="page-container">
      <% unless action_name == "new" %>
        <div id="content-wrap">
      <% else %>
        <div>
      <% end %>
        <% unless action_name == "welcome"%>
          <% unless content_for? :navbar %>
            <%= render 'shared/navbar' %>
          <% end %>
          <%= render 'shared/flashes' %>
        <% end %>

        <%= yield %>
      </div>

      <% unless action_name == "new"%>
        <% unless content_for? :footer %>
          <footer id="footerbottom" class="my-4">
            <div class="footer-links">
              <a href="#"><i class="fab fa-instagram"></i></a>
              <a href="#"><i class="fab fa-facebook"></i></a>
              <a href="#"><i class="fab fa-twitter"></i></a>
            </div>
            <div class="footer-links">
              <p class="mx-2"> <%= link_to "Help", help_path, class: "footer"%>  </p>
              <p class="mx-2" > <%= link_to "About", about_path, class: "footer" %></p>
              <p class="mx-2"> <%= link_to "Contact", contact_path, class: "footer" %></p>

            </div>
            <p class="subtitles"> ® Ready Meals 2021</p>

          </footer>
        <% end %>
      <% end %>
    </div>
  </body>
</html>
